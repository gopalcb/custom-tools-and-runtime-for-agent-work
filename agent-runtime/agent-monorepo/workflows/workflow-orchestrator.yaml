version: 1

workflows:
  workflow-selection:
    phase: planning
    steps:
      - ref: pre-work-health
      - ref: analyze
      - ref: resolve-context
      - ref: web-research
      - ref: plan

  codex-smoke:
    phase: execution
    steps:
      - ref: pre-work-health
      - ref: execute
      - ref: store-strategy-memory
      - ref: finalize

  default:
    phase: execution
    steps:
      - ref: pre-work-health
      - ref: execute
      - ref: validate
      - ref: store-strategy-memory
      - ref: finalize

  implementation-planning:
    phase: execution
    steps:
      - ref: pre-work-health
      - ref: store-strategy-memory
      - ref: finalize

  build-agent:
    phase: execution
    steps:
      - ref: pre-work-health
      - ref: design
      - ref: ui
      - ref: agent-validate
      - ref: store-strategy-memory
      - ref: finalize

"""Workflow resolution, execution, and shared workflow data models.

The package keeps workflow mechanics inside the shared runtime. The public
compatibility imports remain available from ``agent_monorepo.workflow``.
"""

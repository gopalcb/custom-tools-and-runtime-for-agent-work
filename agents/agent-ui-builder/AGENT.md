# Agent UI Builder

Create simple, understandable user interfaces for agent workflows.

V1 constraints:
- Prefer a standalone HTML/CSS/JavaScript mockup over a frontend framework.
- Keep dependencies at zero unless the user explicitly requests otherwise.
- Optimize the interface around fast agent creation: name, purpose, context, optional workflow, skills, restrictions, preview, build.
- Do not implement backend/runtime responsibilities in the UI project.

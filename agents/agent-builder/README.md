# Agent Builder V1.0

V1 agent that converts a small build request into a validated, versioned agent scaffold under `agents/agent-<name>/`.

The Agent Builder owns **design**. The shared Agent Gateway owns **materialization, loading, and execution**.

See repository-root `IMPLEMENTATION.md` and `ARCHITECTURE.md` for setup and extension rules.

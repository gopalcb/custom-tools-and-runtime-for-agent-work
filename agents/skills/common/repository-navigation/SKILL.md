# Repository Navigation

Inspect the smallest relevant portion of the repository first. Reuse existing abstractions before introducing new ones. Avoid repository-wide reads when project or file scope is already known.

# Agent Validation

Validate identifiers, required instructions, skill references, runtime/provider configuration, permissions, destination paths, and initial evaluation criteria before registering an agent.

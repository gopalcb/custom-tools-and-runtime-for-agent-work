# Agent Design

Design agents around a single clear purpose, bounded responsibilities, a predictable workflow, reusable skills, minimal permissions, and explicit completion criteria. Shared runtime behavior belongs in Agent Gateway, not individual agents.

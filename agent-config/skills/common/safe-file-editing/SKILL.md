# Safe File Editing

Keep edits inside the requested scope, preserve existing behavior unless explicitly changing it, avoid destructive overwrites by default, and validate generated machine-readable files before committing them to the workspace.

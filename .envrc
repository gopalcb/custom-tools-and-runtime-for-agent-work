for directory in ${PATH//:/ }; do
    candidate="$directory/codex"
    if [ "$candidate" != "$PWD/bin/codex" ] && [ -x "$candidate" ]; then
        export CODEX_NATIVE_BIN="$candidate"
        break
    fi
done

PATH_add "$PWD/bin"
